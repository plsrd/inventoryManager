extends layout 

block content 
  h1= category.name 

  p= category.description

  ul 
    each item in category_items 
      li  
        a(href=item.url) #{item.name}
        p #[strong SKU: ] #{item._id}

    else 
      li there are no items